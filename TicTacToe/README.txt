This was a test, trying to get Eclipse sync working.  

Well, it didn't quite work...  GitHub synced a few files it should not have, such as .DS_Store (mac file)

For future reference, all Mac users need to have the .gitignore contain *.DS_Store to prevent it from syncing. 